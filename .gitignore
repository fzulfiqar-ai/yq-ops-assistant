# Secrets — never commit real credentials
.env
.env.*
!.env.example
!web/.env.market

# YQ Bahrain commercial data — never commit
data/
Focus ERP Data/
Focus ERP Updated Reports/
Order received/
New order*/
Planning */
Shipments/
roadmap_sources/
Vfan Item List And Pricing/
Unique Items/
New vendor shotlist Accessories/
vendor_research/
MT Pricing May 2026.xlsx
docs/*.docx
# Focus exports / data spreadsheets dropped anywhere in the repo
*.xlsx
*.xls
# Local logs + throwaway diagnostic scripts
*.log
scripts/_*.py

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# n8n exports may contain credentials — only sanitized workflow JSON is committed
n8n_workflows/*.local.json

# Brand-film / marketing media — large binaries, untracked and previously UNIGNORED,
# so `git add .` would have committed ~66 MB of media and personal photos (06-Sep-2026).
film/
footage/
assets/
exports/
generations.csv
/Furqan*.png
/Furqan*.jpeg
/Furqan*.jpg
# Business-data tree (originals stay on disk; never in git)
business_data/

# --- commercial source data must NEVER be committed (repo is PUBLIC) ---
# 12 Focus exports were tracked from before these rules existed and were publicly
# downloadable from raw.githubusercontent.com until 15-Sep-2026. gitignore does not
# untrack anything already in the index -- that needed `git rm --cached`.
Focus ERP*/
costs/
business_data/
*.xlsm
*.zip
# Dead Railway config, deleted in e947e3f but still present on disk.
/railway.toml
# OneDrive conflict copies land right beside the file they duplicate.
*-Conflict.*
*_conflict*

# Design references the owner sent on WhatsApp (23 MB of screenshots + screen recordings used for
# the v3 redesign). Reference material, not source: it must not land in a PUBLIC repo, and a
# `git add -A` during a redesign round would take the whole folder.
/Design Example/

# wrangler state written by scripts/deploy_cf.py / npx wrangler deploy
web/.wrangler/

# Owner drop folders (dated, e.g. 210926/ = 21-Sep-2026): supplier invoices, packing lists,
# MRN XML, product photos. Only *.xlsx/*.zip were caught before -- the .pdf/.xml/.rar/.jpg
# inside were one `git add -A` away from the PUBLIC repo (21-Sep-2026).
/[0-9][0-9][0-9][0-9][0-9][0-9]/
*.rar
# DB backups written by scripts/db_backup.py live under business_data/ (already ignored) --
# listed again so a moved backup folder can never be committed.
backups/
