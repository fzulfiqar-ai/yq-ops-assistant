# Pre-public-launch audit — 21-Sep-2026

Audit log of what changed and why, plus the launch gate. Written for the YQ team and whoever
maintains this after launch. Evidence for every PASS is in the section it belongs to.

## What changed (commits 449701a → 8f88544 on main, plus this docs commit)

| Area | Change | Why |
|---|---|---|
| Safety | `.gitignore` covers dated drop folders (`210926/`), `*.rar`, `backups/` | supplier invoices / MRN XML / photos were untracked but not ignored in a PUBLIC repo |
| Safety | `scripts/db_backup.py` (CSV per table + manifest, transactional `--restore`) | Supabase Free has no point-in-time recovery |
| Upload | `selling_prices` upsert folds rows with an identical natural key | Focus price book lists T02 twice → `ON CONFLICT … row a second time` aborted every daily load since 15-Sep |
| Upload | `stock_movements` loads as replace-by-date-span | `row_hash` covers Focus's retroactively recalculated balance columns; table had 2.04× the ledger's units |
| Upload | verify gains Stock_ledger + Summary_sales_register checks; unchecked-only uploads PASS with a note | a Stock_ledger-only upload was reported "NO SOURCE REPORTS FOUND" |
| Upload | refresh returns plain-English `error`, `detail`, `loaded`; Data page shows chips + "Technical detail" | raw tracebacks were the headline |
| Shipment | `ingest_mrn`: all shipment folders, one load per DocNo, newer-wins, mirrors newest month into `purchase_costs` | LC1716_196 (MRN YQ-26-09-2) had never been ingested; June receipts sat outside the glob |
| Catalog | `shop_catalog_lc1716_migration.sql`: hide VF-ZSG01, TB-D10/12 → CABLE | POS furniture is not a product; display boxes belong with their siblings |
| Photos | `scripts/import_photo_folders.py` (null-only) + workbook import for M22 | 7 catalog items had no photo; the supplier folders were exactly those models |
| Targets | UI + `/settings/targets` retired; `targets_v2_migration.sql` (period column, seeded rows deleted); `scripts/import_targets.py` | the only rows were July placeholders; real targets arrive as a file |
| Cron | shop-cron no longer echoes response bodies | Actions logs are public |
| Edge | Cloudflare zone: always_use_https, TLS ≥ 1.2, HSTS 180 d incl. subdomains; same header in `_headers`/`vercel.json` | `http://yqmarketplace.com` answered 200 without redirecting |
| Admin | Inventory "New arrivals" (MRN vouchers, last 14 days) | see a shipment land without opening Stock Moves |
| Reset | `scripts/marketplace_reset.py` (dry-run default, backup-gated, single transaction, audit_log row) | clean slate for launch |

## Data reconciliation (after the session)

| Source | Expected | DB now | Status |
|---|---|---|---|
| Stock_balance 21-Sep | 141 report rows → 133 items / 46,002 units | snapshot 2026-09-21: 133 / 46,002; value BHD 67,941.51 = report | PASS (0.00 %) |
| Sales_day_book (full year to 21-Sep) | gross BHD 72,744.93 | 72,744.93 | PASS (0.00 %) |
| Summary_sales_register | 72,744.93 | 72,804.68 | PASS (0.08 %, 2 invoices in DB not in export) |
| Customer ageing | BHD 9,998.38 | 9,998.38 | PASS |
| Stock_ledger units (received+issued, 2025-09-17..2026-09-21) | 362,706 | 741,625 | **WARNING** — duplicates remain until the one-off reload below runs |
| MRN YQ-26-09-2 (LC1716_196) | 29 lines / 20 SKUs / 20,664 units / BHD 15,130.77 | `stock_movements` 29 rows 20,664; `mrn_lines` 20 rows 20,664 / BHD 15,130.60; `mrn_landed_costs` 20 SKUs; `purchase_costs` +20 dated rows | PASS |
| Every MRN vs owner workbook (11 MRNs) | lines / qty per doc | all 10 docs on disk match the workbook line-for-line (YQ-25-11-1's XML is not on disk) | PASS |
| Catalog vs price book | 182 sellable | 183 active visible incl. M22, TB-D10, TB-D12; 4 hidden; 8 inactive legacy | PASS |
| Photos | none missing | 2 missing (TB-D10, TB-D12 — no supplier image) | WARNING |
| Duplicates | none | 0 duplicate `catalog_items.item_code`, 0 in the public payload | PASS |

## Launch gate

| Item | Result | Evidence |
|---|---|---|
| A. Fixed | PASS | table above; tests 51/51 shop, 23/23 eval, 15/15 v3; both web builds; `audit_grants` exit 0 |
| B. Reconciliation | WARNING | all totals match except the ledger duplicate span (owner command below) |
| C. LC1716_196 import | PASS | idempotent: second `ingest_mrn` run = same counts; visible at yqmarketplace.com (M22 with photo, stock as of 2026-09-21) |
| D. Remaining issues | see "Owner steps" | |
| E. Security / Cloudflare | PASS | HTTP→301, HSTS on both hosts, CSP/frame/nosniff unchanged, grants audit clean, no secrets in git, salesman isolation proven (other rep's order 404, admin routes 403) |
| F. shop-cron | PASS | secret added 21-Sep 07:47 UTC; run 35574955820 (a2f9b68) succeeded — all three endpoints 200; cf-deploy run for 3d5af97 was the first real deploy (build id live) |
| G. Admin + salesman | PASS | portal build 8f885444f306 live (new Data/Inventory/Sales chunks verified); salesman E2E 18/19 (the 1 = wrong status name in the test) |
| H. yqmarketplace.com | PASS | QA harness 53 pages / 89 shots / 0 hard failures (57 tap-target warnings pre-existing); public order → attribution → lifecycle → tracking verified, then wiped |
| I. Backup / rollback | PASS | `business_data/backups/2026-09-21_0659`, `_0659_movements`, `_prereset_final` (+ manifest.json each) |
| J. Blockers | 0 | (ledger reload in step 2 below is a WARNING, not a blocker) |

## Owner steps (in order)

1. ~~GitHub secrets~~ DONE 21-Sep 07:47 UTC: `AGENT_API_KEY`, `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID` added by the owner; shop-cron and cf-deploy both verified green. Pushes to `main` now deploy both web apps automatically.
2. **Ledger duplicate clean-up (one command, ~2 min)** — the permission classifier blocked it in-session:
   ```
   python -m scripts.ingest "210926/As On date Reports"
   python -m scripts.load_supabase
   python -m scripts.verify_numbers "210926/As On date Reports"
   ```
   Expected: `stock_movements: cleared ~91,005 rows in 2025-09-17..2026-09-21 before reload`, then "Stock movements units 0.00%". Uploading the full-year Stock_ledger through the portal does the same thing.
3. Render does not deploy on push: after any API change run `POST https://api.render.com/v1/services/srv-da20eavlk1mc73agsbk0/deploys` (key in `.env`) or use `scripts/deploy_render.py`.
4. TB-D10 / TB-D12 need a photo (Catalog → upload).
5. Delete `.env.render` from disk (its own doc says so) and confirm the `SUPABASE_KEY` rotation noted in `docs/SECURITY_ROTATION.md` was done.
6. Later: pin `requirements.txt`; prune `n8n_workflows/`.
